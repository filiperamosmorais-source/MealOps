services:
  postgres:
    image: postgres:16
    container_name: mealops-postgres
    environment:
      POSTGRES_USER: mealops
      POSTGRES_PASSWORD: mealops
      POSTGRES_DB: mealops
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
